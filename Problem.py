'Project 3: Advanced Numerical Algorithms in Python'

import numpy as np


class Problem:
    def __init__(self, width, length, delta_x, delt_y):
        """
        Initializes class Problem with given values.
        
        Parameters:
            width: float, width
            length: float, length
            delta_x: float, stepsize of the grid
            delta_y: float, stepsize of the grid

        """

    def update_boundary(new_values: np.array, i:int):
        '''
        Updates the boundary condition with new_values for the boundary i.
        
        Parameters:
            new_values: np.array - new boundary condition
            i: int, index of the boundary condition we need to change
        
        Returns:
        -
        '''

        